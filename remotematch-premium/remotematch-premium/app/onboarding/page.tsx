"use client";
import { useState } from "react"; import Link from "next/link"; import { ArrowRight, UploadCloud } from "lucide-react";
const steps=[['What are you looking for?',['Product Designer','Software Engineer','Product','Growth']],['Where can you work?',['Worldwide','India','US','Europe','Selected countries']],['What matters most?',['Full-time','Contract','Senior','Staff / Lead']],['Core skills',['TypeScript','React','Product strategy','Analytics']],['Resume','Upload or paste your latest resume']];
export default function Onboarding(){const [step,setStep]=useState(0); const [selected,setSelected]=useState(''); const s=steps[step]; return <main className="page"><div className="container max-w-2xl py-10"><Link href="/" className="text-sm muted">← BYN</Link><div className="mt-10 flex items-center justify-between"><div><p className="text-sm font-semibold text-[#087f70]">Profile setup</p><p className="mt-1 text-sm muted">One decision at a time</p></div><span className="mono text-sm muted">{step+1} / 5</span></div><div className="mt-4 flex gap-2">{steps.map((_,i)=><span key={i} className={`h-1.5 flex-1 rounded-full ${i<=step?'bg-[#087f70]':'bg-[#e7e7e3]'}`}/>)}</div><section className="soft-card mt-8 p-6 sm:p-8"><h1 className="text-3xl font-semibold tracking-tight">{s[0]}</h1><p className="mt-2 text-sm leading-6 muted">{step===4?'Use evidence we can verify when we evaluate opportunities.':'Choose what best describes your current search.'}</p>{step<4?<div className="mt-7 grid gap-3 sm:grid-cols-2">{(s[1] as string[]).map(x=><button onClick={()=>setSelected(x)} className={`rounded-2xl border p-4 text-left transition ${selected===x?'border-[#8fc9c1] bg-[#e6f4f1]':'border-[#e7e7e3] bg-white hover:bg-[#f7f7f5]'}`} key={x}><span className="font-medium">{x}</span><span className="mt-1 block text-sm muted">Select this preference</span></button>)}</div>:<div className="mt-7 rounded-2xl border border-dashed border-[#cfcfca] bg-[#f7f7f5] p-10 text-center"><UploadCloud className="mx-auto text-[#087f70]"/><h2 className="mt-3 font-semibold">Drop your resume here</h2><p className="mt-1 text-sm muted">PDF or paste text</p><button className="soft-button primary mt-5">Choose resume</button></div>}<button onClick={()=>setStep(v=>Math.min(4,v+1))} className="soft-button primary mt-6 flex w-full items-center justify-center gap-2">{step===4?'Finish profile':'Continue'} <ArrowRight size={16}/></button></section></div></main>}
